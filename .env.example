# ArgoCD admin password - must be a bcrypt hash
# Generate: python3 -c "import bcrypt; print(bcrypt.hashpw(b'yourpassword', bcrypt.gensalt(rounds=10)).decode())"
ARGOCD_ADMIN_PASS=

# Cloudflare API token for DNS management
CLOUDFLARE_API_TOKEN=

# Samba password for the przemek user
SAMBA_PASSWORD_PRZEMEK=
