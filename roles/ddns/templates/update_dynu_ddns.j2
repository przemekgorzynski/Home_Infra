MY_IP=$(curl -s ipinfo.io/ip)

# update IP
echo "Updating gorillabay.click"
curl "https://{{ dynu_username }}:{{ dynu_password }}@api.dynu.com/nic/update?hostname={{ domain }}&myip=$MY_IP"
echo

sleep 10
echo "Updating pszemazzz.com"
curl "https://{{ dynu_username }}:{{ dynu_password }}@api.dynu.com/nic/update?hostname={{ domain2 }}&myip=$MY_IP"
